#production
deployment:
  user: dummy
  from: ./
  to: /var/www/vhosts/example.com/www
  excludes:
releases:
  enabled: true
  max: 10
  symlink: current
  directory: releases
hosts:
  - server-host
tasks:
  pre-deploy:
  on-deploy:
  post-release:
  post-deploy: